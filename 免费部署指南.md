# 项目评分系统 - 云端部署配置

## 🆓 免费部署方案选择

### 方案对比

| 平台 | 免费额度 | 数据库 | 域名 | 部署难度 | 推荐度 |
|------|----------|--------|------|----------|--------|
| **Render** | 750小时/月 | 有(需单独申请) | your-app.onrender.com | ⭐⭐ | ⭐⭐⭐⭐⭐ |
| **Railway** | $5信用/月 | 内置 | your-app.railway.app | ⭐ | ⭐⭐⭐⭐ |
| **PythonAnywhere** | 500MB磁盘 | 有限 | yourname.pythonanywhere.com | ⭐⭐⭐ | ⭐⭐⭐ |

## 🚀 选择Render的理由

1. **完全免费** - 750小时/月，足够持续运行
2. **Docker原生支持** - 适合我们的容器化应用
3. **HTTPS自动配置** - 免费SSL证书
4. **简单易用** - Web界面操作，无需复杂配置
5. **可扩展** - 后续可升级到付费计划

## 📋 部署步骤

### 第一步：准备部署文件

#### 1. 创建精简Dockerfile

```dockerfile
# Dockerfile.render
FROM python:3.9-slim

WORKDIR /app

# 安装系统依赖
RUN apt-get update && apt-get install -y \
    gcc \
    g++ \
    && rm -rf /var/lib/apt/lists/*

# 复制依赖文件
COPY requirements.txt .

# 安装Python依赖
RUN pip install --no-cache-dir -r requirements.txt

# 复制应用代码
COPY backend/ ./backend/
COPY config.py .
COPY start_server.py .

# 暴露端口
EXPOSE 8000

# 启动命令
CMD ["python", "start_server.py"]
```

#### 2. 创建启动脚本

```python
# start_server.py
import uvicorn
from backend.app import app

if __name__ == "__main__":
    print("🚀 项目评分系统启动中...")
    print("🔗 访问地址: https://your-app.onrender.com")
    print("📚 API文档: /docs")
    print("💪 健康检查: /health")
    
    uvicorn.run(
        app,
        host="0.0.0.0",
        port=8000,
        reload=False
    )
```

#### 3. 精简requirements.txt

```txt
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
pymongo==4.6.0
redis==5.0.1
numpy==1.24.4
scikit-learn==1.3.2
python-multipart==0.0.6
```

### 第二步：使用免费的数据库

由于Render的免费数据库需要单独申请，我们先使用SQLite作为免费替代方案。

#### 创建SQLite适配器

```python
# backend/database_sqlite.py
import os
from sqlalchemy import create_engine
from sqlalchemy.ext.declarative import declarative_base
from sqlalchemy.orm import sessionmaker

# SQLite数据库路径
DATABASE_URL = "sqlite:///./projects.db"

engine = create_engine(
    DATABASE_URL, 
    connect_args={"check_same_thread": False}
)

SessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)
Base = declarative_base()

def get_db():
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()
```

### 第三步：配置环境变量

```python
# config_render.py
import os

class Settings:
    # 应用配置
    APP_NAME = "项目评分系统"
    DEBUG = os.getenv("DEBUG", "False").lower() == "true"
    
    # 数据库配置
    DATABASE_URL = os.getenv("DATABASE_URL", "sqlite:///./projects.db")
    
    # Redis配置（可选）
    REDIS_URL = os.getenv("REDIS_URL", None)
    
    # 安全配置
    SECRET_KEY = os.getenv("SECRET_KEY", "your-secret-key-change-in-production")
    
    # CORS配置
    ALLOWED_ORIGINS = os.getenv("ALLOWED_ORIGINS", "*").split(",")

settings = Settings()
```

## 📝 在Render部署的完整步骤

### 1. 注册Render账户
- 访问 https://render.com
- 使用GitHub或邮箱注册
- 验证邮箱

### 2. 准备GitHub仓库
- 将项目代码推送到GitHub
- 确保包含以下文件：
  - `Dockerfile.render` (重命名为Dockerfile)
  - `requirements.txt`
  - `backend/` 目录
  - `start_server.py`
  - `render.yaml` (部署配置)

### 3. 创建Render部署配置

```yaml
# render.yaml
services:
  - type: web
    name: project-rating-system
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    plan: free
    envVars:
      - key: DEBUG
        value: false
      - key: PORT
        value: 8000
      - key: PYTHONUNBUFFERED
        value: 1
    healthCheckPath: /health
    autoDeploy: true
```

### 4. 在Render控制台部署
1. 点击"New +" → "Web Service"
2. 连接GitHub仓库
3. 自动检测Docker配置
4. 设置服务名称：`project-rating-system`
5. 选择免费计划（Free）
6. 点击"Create Web Service"

### 5. 等待部署完成
- 大约5-10分钟构建
- 自动获取域名：`project-rating-system.onrender.com`
- 自动配置HTTPS

## 🔗 访问您的应用

部署完成后，您可以访问：

1. **主应用**：https://project-rating-system.onrender.com
2. **API文档**：https://project-rating-system.onrender.com/docs
3. **健康检查**：https://project-rating-system.onrender.com/health

## 📊 免费限制说明

### Render免费计划：
- ✅ 750小时/月（约31天连续运行）
- ✅ 512MB RAM
- ✅ 共享CPU
- ✅ 免费HTTPS
- ✅ 自定义域名（需付费）
- ❌ 没有专用数据库（可使用SQLite）
- ❌ 睡眠策略：15分钟无流量后休眠

### 应对休眠策略：
1. 配置健康检查定期唤醒
2. 使用外部监控服务ping应用
3. 访问频率保持在15分钟内一次

## 🛠️ 监控和维护

### 免费监控工具：
1. **UptimeRobot** - 免费监控，每5分钟ping一次
2. **Healthchecks.io** - 免费Cron监控
3. **Google Analytics** - 免费访问统计

### 基本维护：
1. 定期检查Render控制台
2. 查看日志排查问题
3. 备份SQLite数据库文件

## 💡 优化建议

### 减少内存使用：
1. 使用精简Python镜像
2. 关闭开发模式
3. 优化依赖包

### 提高响应速度：
1. 启用缓存
2. 压缩静态资源
3. 优化数据库查询

## 🚨 故障排除

### 常见问题：
1. **应用无法启动** - 检查日志中的错误信息
2. **数据库连接失败** - 检查SQLite文件权限
3. **内存不足** - 升级到更高计划或优化代码
4. **HTTPS错误** - 等待Render自动配置证书

## 📞 技术支持

如需帮助：
1. 查看Render文档：https://render.com/docs
2. 查看应用日志
3. 在GitHub提交Issue

---

## ✅ 开始部署！

**建议操作顺序：**
1. 将项目推送到GitHub
2. 在Render创建Web Service
3. 连接GitHub仓库
4. 等待自动部署
5. 测试访问地址

**预计时间**：15-30分钟

**总成本**：$0.00 💰

**在线状态**：24/7可用（有休眠策略）

---

🎉 **恭喜！您的项目评分系统即将免费上线！**